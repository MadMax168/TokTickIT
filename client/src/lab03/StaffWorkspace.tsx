import { useEffect, useState } from 'react'
export default function StaffWorkspace({path,onNavigate}:{path:string;onNavigate:(p:string)=>void}){const [items,setItems]=useState<any[]>([]),[error,setError]=useState('');useEffect(()=>{if(path==='/staff/tickets')void fetch('/api/staff/tickets').then(async r=>{if(!r.ok)throw Error('Queue unavailable.');setItems((await r.json()).items)}).catch(e=>setError(e.message))},[path]);if(path!=='/staff/tickets')return <section><h1>Access unavailable</h1><button onClick={()=>onNavigate('/staff/tickets')}>Return to Queue</button></section>;return <section><h1>Ticket Queue</h1>{error&&<p role="alert">{error}</p>}{items.length===0?<p>No tickets found.</p>:<table><thead><tr><th>Ticket</th><th>Summary</th><th>Requested Priority</th><th>IT Priority</th><th>Status</th><th>Owner</th></tr></thead><tbody>{items.map(t=><tr key={t.id}><td>{t.ticketNumber}</td><td>{t.summary}</td><td>{t.requestedPriority}</td><td>{t.itPriority}</td><td>{t.currentStatus}</td><td>{t.assignedTo?.name??'Unassigned'}</td></tr>)}</tbody></table>}</section>}
